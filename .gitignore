# 1. 数据集文件夹 (图片非常大，绝对不传)
datasets/

# 2. Python 虚拟环境 (别人克隆代码后会自己装，不用传)
carla_venv/
venv/
env/

# 3. 训练出来的模型权重文件 (动辄几十上百MB，不传)
*.pth
*.pt

# 4. Python 缓存和编译文件 (没用的垃圾文件)
__pycache__/
*.pyc

# 5. 操作系统自动生成的隐藏文件
.DS_Store